$(document).ready(function(){
  $('#slider').slick({
    centerMode: true,
    centerPadding: '160px'
  // accessibility: true,
  // autoplay: true,
  // autoplaySpeed: 1000,
  // dots: true,
  // fade: true,
  });
});

